-- Keycloak은 운영 모드에서 실제 DB를 요구한다. dev 모드의 내장 H2는 지원되지 않는다.
-- 애플리케이션 데이터(llmhub)와 인증 데이터(keycloak)는 같은 인스턴스의 다른 데이터베이스에
-- 둔다. 스키마가 섞이지 않고, 나중에 분리할 때 옮기기만 하면 된다.
--
-- 이 스크립트는 데이터 디렉토리가 비어 있을 때 한 번만 실행된다(postgres 이미지 규약).
create database keycloak;
